# 144基站定位压测环境迁移与联调说明

迁移日期：2026-08-06  
源环境：`192.168.173.167`  
目标环境：`192.168.169.144`

## 1. 迁移结论

定位服务已从167在线复制到144，167全程未停止，目前两套环境同时运行。144使用独立目录、独立数据库卷、独立RabbitMQ卷和独立随机凭据，不会消费或修改167的数据与消息。

| 项目 | 144结果 |
|---|---|
| 部署目录 | `/home/twai/wjx/base-station-location` |
| CPU架构 | `x86_64/amd64` |
| 定位HTTP | `http://192.168.169.144:18080` |
| PostGIS | `192.168.169.144:15432/dispatch_assist` |
| RabbitMQ | `192.168.169.144:5672`，vhost `/location` |
| 管理端口 | `127.0.0.1:15672`，只允许服务器本机或SSH隧道 |
| 服务状态 | 定位服务`UP`，PostGIS/RabbitMQ健康 |
| JAR SHA-256 | `d40c03b1e86a91db7f703e47ec66bba68e0ea239db043cdae6eff8352d3fec4e` |

144原有地址机器人使用`5187`，定位组件使用`18080/15432/5672`，端口不冲突。

## 2. 架构与迁移方式

167为`aarch64`，144为`x86_64`，因此没有复制167目录中的ARM64镜像包。144通过可达的镜像加速地址取得以下`amd64`基础镜像，并在本机构建PostGIS和定位应用镜像：

- `postgres:18-alpine`；
- `rabbitmq:4.3.4-management-alpine`；
- `eclipse-temurin:17-jre-jammy`；
- `wjx-location-postgis:18-3.6`；
- `wjx-location-service:1.0.0`。

数据库使用PostgreSQL自定义格式在线一致性快照迁移。RabbitMQ没有复制167的历史消息，144创建全新交换机、双下游队列、死信队列和独立账号，更适合后续压测。

## 3. 数据快照证据

| 项目 | 值 |
|---|---:|
| 快照时间 | `2026-08-06 03:37:30.260801+00` |
| 压缩备份大小 | 34,653,920字节 |
| 备份SHA-256 | `498c5b441d9792f4a5050b5deeedb2ca987a73296a159101599ea7dfc9cdda43` |
| `dispatch_assist.address_inventory` | 99,000条 |
| `ai.poi_3` | 73,665条 |
| `ai.aoi_3` | 1,270条 |
| 144恢复后PostgreSQL卷 | 约523.4 MB |

备份在167和144均保留，144路径为：

```text
/home/twai/wjx/base-station-location/runtime/migration-144/location-144.dump
```

当前迁移数据是167实际运行数据的快照，不是百万条数据集。若本轮压测要求百万地址规模，应先在144单独扩充测试数据并重新执行`ANALYZE`，不能把99,000条结果标记成百万级压测。

## 4. 端到端验收结果

使用CTI坐标`116.33,39.92`和2公里半径在144执行`e2e-smoke.sh`：

| 指标 | 结果 |
|---|---:|
| API坐标到`scopeId` | 19.564 ms |
| `scopeId` | `672fc095-9ec8-4385-88f4-0ff4bd6043f8` |
| Outbox到Broker确认 | 671.125 ms |
| 双队列事件 | 均完成CloudEvent与`locationScope`消息体校验 |
| 双只读账号 | 查询同一逻辑子库数量一致 |
| 逻辑子库命中总数 | 35,985条 |
| AOI / BUILDING / LOI / POI | 1,410 / 1,847 / 5,956 / 26,772 |
| 全部验收动作 | 1,607 ms |
| Outbox | `PUBLISHED`，重试0次 |
| 死信 | 两个DLQ均为0 |
| 验收后队列基线 | 两个主队列和两个DLQ均为0 |

全部验收动作包含MQ轮询、消息体检查、双账号完整计数和分类统计，不能当作定位API或下游单次业务SQL的时延。

空闲复测时RabbitMQ CPU为0.18%、定位服务0.69%、PostGIS 0.06%；首次资源快照中的高CPU是RabbitMQ启动和账号初始化瞬时消耗，诊断结果显示无告警、调度队列为0。

## 5. 运维与联调命令

```bash
cd /home/twai/wjx/base-station-location

bash status.sh
bash verify.sh
bash verify-deployment.sh

# 会产生一次模拟定位和两条队列消息
bash e2e-smoke.sh
```

从其他服务器访问：

```text
HTTP:     192.168.169.144:18080
PostGIS:  192.168.169.144:15432
RabbitMQ: 192.168.169.144:5672
```

账号密码只保存在144的`/home/twai/wjx/base-station-location/.env`，文件权限为`600`，不写入代码、文档或MQ消息。热词服务和地址机器人必须分别领取自己的MQ及数据库只读账号。

## 6. 压测前检查

1. 明确压测数据规模：当前为99,000条统一地址记录；
2. 明确压测入口：定位API、`scopeId`数据库查询或包含下游消费的完整链路；
3. 验收产生的4条测试消息已从两个主队列清理；压测前再次确认四个队列均为0；
4. 监控定位JVM、PostGIS连接数与查询耗时、RabbitMQ发布确认和队列积压；
5. 144根分区当前剩余约7.2 GB，不宜无限增加镜像、构建缓存和压测日志；
6. 当前为单机联调/压测环境，不代表生产高可用架构。

## 7. 回退与并存

167仍提供原地址：

```text
HTTP:     192.168.173.167:18080
PostGIS:  192.168.173.167:15432
RabbitMQ: 192.168.173.167:5672
```

144联调失败时，下游只需继续使用167的连接参数，无需执行数据库回滚。未明确切换前，不停止167，不把144配置覆盖到167。
